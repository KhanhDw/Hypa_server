# Các gói bạn đã ghi
fastapi==0.115.4
uvicorn[standard]==0.32.0
httpx==0.28.1
beautifulsoup4==4.14.3
lxml==6.0.2
cachetools==6.2.2

---

# Bổ sung các gói quan trọng khác

# Database/ORM
asyncpg==0.30.0  # PostgreSQL async driver
SQLAlchemy==2.0.36  # Object-Relational Mapper (ORM)

# Task Queue (Celery)
celery==5.4.0
redis==5.2.0  # Thường dùng làm broker/backend
amqp==5.3.1
kombu==5.6.1

# Bảo mật và Xác thực (Authentication)
bcrypt==5.0.0  # Hashing mật khẩu
passlib==1.7.4
python-jose==3.3.0  # Hỗ trợ JWT
cryptography==43.0.1  # Thư viện mật mã cấp thấp

# Input/Output & Validation
aiofiles==23.2.1  # File I/O bất đồng bộ
email_validator==2.2.0
python-multipart==0.0.20  # Hỗ trợ form data
python-dotenv==1.0.1  # Quản lý biến môi trường
PyYAML==6.0.3  # Hỗ trợ cấu hình/data

# Pydantic & Asyncio
pydantic==2.9.2
pydantic-settings==2.6.1
anyio==4.12.0
websockets==15.0.1

# Template Engine (nếu bạn render HTML)
Jinja2==3.1.4